<nav class="navbar navbar-expand-lg navbar-dark bg-dark container px-3">
  <a class="navbar-brand" href="<%= root_path %>">Image App</a>
  <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <% if user_signed_in? %>
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item active">
          <%= link_to("Images", images_path, class: "nav-link") %>
        </li>
        <li class="nav-item">
          <%= link_to("Upload Image", new_image_path, class: "nav-link") %>
        </li>
      </ul>
    <% end %>

    <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
      <% if current_user %>
        <li class="nav-item d-flex align-items-center">
          <%= link_to ('<i class="fa fa-user"></i> ' + truncate(current_user.email, length: 30)).html_safe, edit_user_registration_path, class: "nav-link", title: 'Edit Profile' %>
        </li>
        <li class="nav-item mx-2"></li>
        <li class="nav-item logout">
          <%= link_to('Logout', destroy_user_session_path, class: 'btn btn-xs btn-danger', title: 'Logout', data: { turbo_method: :delete }) %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to('Sign In', new_user_session_path, class: 'btn btn-outline-light my-2 my-sm-0', title: 'Sign In') %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>